# AI Common Utilities (`ai/common`)

Provides shared Python base classes, Pydantic request/response schemas, API error handlers, logging abstractions, and LLM provider token wrappers.

> Status: Scaffolding baseline.
