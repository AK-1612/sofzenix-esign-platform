# AI Smart Search Submodule (`ai/smart_search`)

Generates vector embeddings for document chunks, maintains vector store indexes, and executes semantic hybrid retrieval over tenant document repositories.

> Status: Scaffolding baseline.
