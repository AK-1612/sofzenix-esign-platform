# AI OCR Submodule (`ai/ocr`)

Handles document rasterization, image pre-processing, page deskewing, and optical character recognition (OCR) fallback for scanned non-searchable PDF documents and images.

> Status: Scaffolding baseline.
