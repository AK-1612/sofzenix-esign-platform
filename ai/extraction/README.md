# AI Key Entity Extraction Submodule (`ai/extraction`)

Extracts structured metadata, entity fields, key-value pairs (effective dates, party names, total contract values, governing law) from unstructured text.

> Status: Scaffolding baseline.
