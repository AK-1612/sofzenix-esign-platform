# Types Directory (`frontend/src/types`)

This directory will hold TypeScript definitions matching domain interfaces:
- `document.ts`: Document status enum, metadata types, signature field coordinates.
- `workflow.ts`: Workflow definition, step transition rules, approval status.
- `user.ts`: User model, Organization hierarchy, Role enum, Permission flags.
- `api.ts`: Standard API paginated response structures and error envelopes.
