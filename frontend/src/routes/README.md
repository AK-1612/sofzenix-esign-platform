# Routes Directory (`frontend/src/routes`)

This directory will hold application navigation configuration:
- Route path constants (`ROUTES.DOCUMENTS`, `ROUTES.WORKFLOWS`).
- RBAC route protection middleware & wrappers (`ProtectedRoute`, `OrgAdminGuard`).
- Navigation bar link structures.
