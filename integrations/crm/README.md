# CRM Integration Submodule (`integrations/crm`)

Intended Purpose: Handles bi-directional synchronization with enterprise CRMs (Salesforce, HubSpot, Zoho, MS Dynamics), triggering document generation on deal status changes and syncing signed contracts back to opportunity records.

> Implementation pending integration contracts and OAuth application setups.
