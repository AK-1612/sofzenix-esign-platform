# Schema DDL Directory (`database/schema`)

Will store baseline DDL scripts (`V1__init_schema.sql`) once domain entities are defined.

> Status: Pending domain modeling phase. No tables or DDL created.
